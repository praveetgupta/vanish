## What this changes

<!-- One or two sentences. Link an issue if there is one. -->

## How it was tested

- [ ] `cd app && swift build -c release` — no warnings
- [ ] Engine starts and behaves in `--mock` mode
- [ ] Tried against a real iPhone — iOS version: <!-- fill in --> , connection: <!-- USB / Wi-Fi -->
- [ ] `engine/cli.py status` / `teleport` / `stop` still work

<!-- If you could not test on a real device, say so — that is fine, it just tells reviewers
     what still needs checking. -->
